server:
  port: 8003

spring:
  application:
    name: trend-detection-service

  jpa:
    hibernate:
      ddl-auto: update

  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/trend_detection
    username: root
    password: Wesley@949
    driver-class-name: com.mysql.cj.jdbc.Driver

  cloud:
    config:
      enabled: false
    import-check:
      enabled: false
